if [ -f ~/.aws ]; then
    . ~/.aws
fi

PS1='\u@\h:\w$(__git_ps1 " (\e[0;32m%s\e[m)")\$ '

# set quicker key repeat rate
# Seems that they break my setting on every macOS restart
#defaults write -g InitialKeyRepeat 15
#defaults write -g KeyRepeat 1.1
